# SGLang Mistral Environment Configuration
# Copy this file to .env and fill in your actual values

# Required: HuggingFace token for model access
# Get this from: https://huggingface.co/settings/tokens
# Make sure it has read access to the model repository
HF_TOKEN=your_huggingface_token_here

# Optional: If using ModelScope instead of HuggingFace
# SGLANG_USE_MODELSCOPE=true

# Optional: Custom paths for model caches
# MODEL_CACHE_PATH=/path/to/your/model/cache

# Optional: Server configuration (defaults to localhost:30000)
SGLANG_HOST=localhost
SGLANG_PORT=30000
